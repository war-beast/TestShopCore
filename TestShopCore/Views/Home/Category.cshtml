@model Category

@{
    ViewBag.Title = Model.Name;
}

<!-- SECTION -->
<div class="section">
    <!-- container -->
    <div class="container">
        <!-- row -->
        <div class="row">
            <!-- ASIDE -->
            <div id="aside" class="col-md-3">
                <div class="checkbox-filter hidden">
                    <input type="checkbox" name="filter" id="category-@Model.Id" data-name="@Model.Name" data-id="@Model.Id" checked />
                </div>
                <div class="aside">
                    <h3 class="aside-title">Цена</h3>
                    <div class="price-filter">
                        <div id="price-slider"></div>
                        <div class="input-number price-min">
                            <input id="price-min" type="number">
                            <span class="qty-up">+</span>
                            <span class="qty-down">-</span>
                        </div>
                        <span>-</span>
                        <div class="input-number price-max">
                            <input id="price-max" type="number">
                            <span class="qty-up">+</span>
                            <span class="qty-down">-</span>
                        </div>
                    </div>
                </div>
                <!-- /aside Widget -->
                <div class="aside">
                    <button id="filter" class="primary-btn order-submit">Применить фильтр</button>
                </div>
            </div>
            <!-- /ASIDE -->
            <!-- STORE -->
            <div id="store" class="col-md-9">
                <!-- store top filter -->
                <div class="store-filter clearfix">
                    <div class="store-sort">
                        <label>
                            Сотрировать по:
                            <select class="input-select" id="sortType">
                                @await Component.InvokeAsync("SortingList");
                            </select>
                        </label>
                    </div>
                </div>
                <!-- /store top filter -->
                <!-- store products -->
                <div class="row" id="productList">
                    @await Component.InvokeAsync("CategoryProducts");
                </div>
                <!-- /store products -->
            </div>
            <!-- /STORE -->
        </div>
        <!-- /row -->
    </div>
    <!-- /container -->
</div>
<!-- /SECTION -->